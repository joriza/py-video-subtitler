# Tareas — add-subtitler-cli

- **Cambio:** `add-subtitler-cli` · **Fase:** tasks · **Fecha:** 2026-09-13 · **Almacén:** openspec
- **Fuentes:** `design.md` (§9 handoff, §3 costuras, §6 estrategia de pruebas), `specs/subtitle-generation/spec.md` (12 requisitos / 31 escenarios), `proposal.md`, `openspec/config.yaml` (`tdd: true`, `test_command: .venv/Scripts/python.exe test_smoke.py`).
- **Disciplina:** TDD estricto en cada costura testeable: **RED → GREEN → TRIANGULATE → REFACTOR** vía `.venv/Scripts/python.exe test_smoke.py` (suite default sin red ni descargas; la sección real `SUBTITLER_SMOKE_REAL=1` entra solo en Fase 5). Cada fase sigue ese ciclo: primero el check que falla, luego la implementación mínima en `subtitler.py`, casos de triángulo, y refactor espejando `convert.py`.
- **Mapa de escenarios:** numeración S1–S33 según `design.md` §6.2; el mapeo escenario→tarea va citado en cada checkbox.

## Review Workload Forecast

| Field | Value |
| ------- | ------- |
| Estimated changed lines | ~1500–1900 (subtitler.py ~900–1150, espejo de convert.py 1229; test_smoke.py ~450–550, espejo de 442; README ~130–180; requirements.txt ~5; .gitignore ~6) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 = Fases 1–2 (soporte + costuras puras C1–C10 con units) → PR 2 = Fases 3–4 (sondeo/extracción/atómica/pipeline/CLI + selector/herramientas, checks default) → PR 3 = Fases 5–6 (transcripción/traducción/progreso opt-in + README + verificación final) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High
```

Cada PR sugerido tiene inicio/fin claros, verificación propia (suite default verde al cierre del PR) y rollback aditivo (`git revert`; `.gitignore` vuelve a solo `.atl/`).

---

## Fase 1 — Soporte del entorno (design §9.1; D9–D12)

- [x] **1.1** Crear `requirements.txt` (D9: `faster-whisper==1.2.1`, `argostranslate==1.11.0`, cabecera de comentario en español) y crear el venv: `python -m venv .venv` con el Python 3.11 de PATH, luego `.venv/Scripts/python.exe -m pip install -r requirements.txt`. Verificar: `.venv/Scripts/python.exe -c "import faster_whisper, argostranslate"` sin error y que el comando declarado `.venv/Scripts/python.exe test_smoke.py` queda ejecutable en forma (aún sin suite). Registrar versión de Python real, tamaño del venv y tiempo de instalación (D10) como dato para el README (tarea 6.1). <!-- sdd-owner: implementation -->
- [x] **1.2** Adaptar `.gitignore` (D11): añadir `.venv/`, `input/`, `__pycache__/`, `tests_tmp/`, `.ruff_cache/` conservando `.atl/`. Verificar con `git check-ignore -v .venv/ input/ tests_tmp/` y que `git status --porcelain` no lista residuos locales. <!-- sdd-owner: implementation -->

## Fase 2 — Costuras puras C1–C10 con unit tests (design §9.2, §3)

Toda la fase: RED = check fallando en `test_smoke.py` (unit en subproceso, patrón `UNIT_SRC` importando `subtitler` real); GREEN = implementación mínima en `subtitler.py`; verificación siempre con `.venv/Scripts/python.exe test_smoke.py`.

- [x] **2.1** Harness del smoke (infraestructura, sin dominio): docstring español con el comando, contadores `passed`/`failed` + `check()` + cierre `RESULT: N passed, M failed` rc 0/1, `TMP = tests_tmp` con `rmtree` en `try/finally`, helper `run_subtitler` (lista de argumentos, `capture_output`, `cwd=ROOT`, stdin/env parcheables) y plantilla `UNIT_SRC`. Verificación: la suite corre y reporta `RESULT: 0 passed, 0 failed`. <!-- sdd-owner: implementation -->
- [x] **2.2** C1 `parse_selection(text, count)` (S28, costura; espejo verbatim del converter): RED (rangos `1 3 5-7` inclusivos, inversión `7-5` normalizada, 1-based→0-based, inválido/fuera de rango → `None`, separadores `[,\s]+`) → GREEN → TRIANGULATE (`2-2`, cadena vacía, solo `a`/`q` queda fuera) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **2.3** C2 `format_srt_timestamp` + C3 `wrap_text` (S17/S18, costuras): RED (`HH:MM:SS,mmm` con coma, clamp de negativos a 0, redondeo a ms; greedy por palabras ≤ `SRT_LINE_MAX_CHARS`=42 sin cortar nunca una palabra) → GREEN → TRIANGULATE (frontera exacta 42, palabra más larga que la línea) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **2.4** C4 `split_cue` + C5 `render_srt` (S17/S18): RED (reparto proporcional al conteo de caracteres, gap mínimo 1 ms, invariante ≤2 líneas/cue afirmado, texto de ~150 chars reconstruido palabra a palabra sin pérdida, numeración `1..n`, `\r\n` único salto, exactamente una línea en blanco entre cues) → GREEN → TRIANGULATE (cue que ya cabe, cue gigante multi-cue) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **2.5** C6 `parse_srt(payload)` (uniformidad S5/S6/S17): RED (strip de BOM, acepta `\r\n` y `\n`, numeración original ignorada, tags `<[^>]+>` eliminados, multi-línea unida con espacio) → GREEN → TRIANGULATE (payload real `mov_text→srt` cuando existan muestras en Fase 3) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **2.6** C7 `resolve_translation_route(src, dst, pairs)` (S14/S15/S16): RED (`src == dst` → direct sin hops; `(src,dst) ∈ pairs` → direct; pivote `de→en→es` con 2 saltos; sin camino → `none`) → GREEN (+ `TranslationRoute` frozen) → TRIANGULATE (pairs vacío, dst distinto de `es`) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **2.7** C8 `decide_extraction` + `pick_subtitle_track` (S5/S6/S7/S8/S9): RED (`--no-extract` → `transcribe_forced`; sin pistas → `transcribe_no_track`; preferida = default marcada, si no primera de texto; `language ∈ {None, es, spa, und}` → `extract`; otro idioma → `extract_translate`; solo bitmap → `transcribe_bitmap`) → GREEN (+ `ExtractionDecision`) → TRIANGULATE (varias pistas, default que no es la primera) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **2.8** C9 `parse_probe_json(payload)` (S1/S2, costura): RED (streams → `SubtitleStream` con `is_text` por allowlist `TEXT_SUBTITLE_CODECS`, `duration_s`, `has_audio`, JSON inválido → `MediaError`; helpers tolerantes `_to_float`/`_safe_int`) → GREEN → TRIANGULATE (duración ausente, codec desconocido cae a bitmap) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **2.9** Registros al tope + validación + dedup (S31/S32, costura; D4/D5): RED unit (`--target en` → error de registro que lista `es`; `--model medium` aceptado del registro; `resolve_inputs` deduplica por `expanduser().resolve()`) → GREEN (`MODELS`/`SOURCE_LANGS`/`TARGET_LANGS`, `ModelSpec`/`LangSpec`, `validate_registry_choices`, `resolve_inputs`) → TRIANGULATE (lang registrado válido, input repetido) → REFACTOR. <!-- sdd-owner: implementation -->

## Fase 3 — Sondeo, extracción, escritura atómica, pipeline y CLI (design §9.3; checks default)

- [x] **3.1** Fábricas lavfi en `test_smoke.py` (infraestructura RED de la fase): `make_video` (`testsrc2` + `sine`, `libx264`+`aac`, un nombre con espacios «sample test.mp4»), `make_embedded` (mux de `.srt` con `-c:s mov_text`, `language=spa|eng`, `-disposition:s:0 default`), video sin audio y archivo de bytes basura `.mp4`. Verificación: las muestras se crean en `tests_tmp/` y se limpian al salir (S1/S2/S3/S5/S9 habilitados). <!-- sdd-owner: implementation -->
- [x] **3.2** Sondeo + fallo aislado por archivo (S1 parcial/S2/S3): RED (basura `.mp4` → `failed` con detalle recortado a `STDERR_TAIL_CHARS` y la corrida sigue; sin streams de audio → `failed` con motivo «sin audio»; ruta con espacios sondeada completa) → GREEN (`probe_media` + esqueleto de `subtitlar_one` que nunca lanza, `MediaError` → failed) → TRIANGULATE (mezcla de ok/failed en una corrida) → REFACTOR. La aserción exacta `TOTAL: 1 ok, 1 failed, 1 skipped` (S25=S1) queda para 3.6, cuando exista el resumen. <!-- sdd-owner: implementation -->
- [x] **3.3** Extracción embebida + formato a nivel de bytes (S5/S17/S19): RED (pista `spa` → `.srt` extraído en segundos sin transcribir; bytes: BOM `EF BB BF`, solo `\r\n`, numeración `1..n`, `HH:MM:SS,mmm --> HH:MM:SS,mmm`, una línea en blanco entre cues; `video.srt` junto al fuente con el mismo stem) → GREEN (`ffmpeg -map 0:<index> -c:s srt -f srt pipe:1` con lista de argumentos → `parse_srt` → `render_srt` → escritura) → TRIANGULATE (tags `<i>` de `mov_text` limpiados por C6) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **3.4** Escritura atómica + SKIP/--force (S20/S21/S22): RED (unit de `write_srt_atomic`: todo a `<out>.tmp` con `utf-8-sig, newline=""` y `os.replace`; S20 tras un fallo no queda `.srt` ni `.srt.tmp`; S21 `.srt` existente → SKIP + WARN y `st_mtime_ns` intacto; S22 con `--force` contenido y mtime cambian) → GREEN (`_remove_quietly` + lógica skip/force en `subtitlar_one`) → TRIANGULATE (skip en dry-run ya cubierto en 3.5) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **3.5** `--dry-run` (S23/S24/S8/S32, plan): RED (línea de plan exacta `[plan] <nombre> → <ruta elegida> → <salida esperada>`; cero `.srt` creados y sin descargas; existente sin `--force` → `SKIP (.srt existe)`; `--no-extract` → «transcribir (--no-extract)»; `--model medium` mencionado en el plan) → GREEN (plan §4.3 sobre sondeo + C8, sin imports pesados) → TRIANGULATE (rutas `extraer pista #N`, `transcribir (sin pistas)`, `transcribir (bitmap…)` vía probes fabricados) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **3.6** CLI + resumen + exit codes (S25/S26/S27/S31): RED (`TOTAL: 1 ok, 1 failed, 1 skipped` exacto + rc 1 en la mezcla S1/S25; solo ok/skipped → rc 0 [S26]; unit que parchea `subtitlar_one` para lanzar `KeyboardInterrupt` y verifica `main([...]) == 130` [S27]; `--target en` → rc 2 con stderr listando `es` [S31]) → GREEN (`build_parser` con `ArgumentDefaultsHelpFormatter` y todas las opciones de la superficie, `main(argv) -> int`, `summarize` + `Tiempo total: HH:MM:SS`, `sys.exit(main())`, `except KeyboardInterrupt → 130`) → TRIANGULATE (rc 0 en dry-run) → REFACTOR. <!-- sdd-owner: implementation -->

## Fase 4 — Selector interactivo y verificación de herramientas (design §9.4)

- [x] **4.1** Selector interactivo (S28/S29/S30): RED (7 muestras con pista `spa` embebida + stdin `1 3 5-7` → exactamente 5 `.srt`; stdin `99 x\nq\n` re-pregunta y no procesa, rc 0; stdin `a\n` procesa todos; stdin `q\n` → cero salidas rc 0; `--input-dir` inexistente → carpeta creada + guía impresa, rc 0) → GREEN (`interactive_select`/`list_videos` sobre C1; creación de carpeta con guía) → TRIANGULATE (rangos invertidos en el selector, selector con carpeta vacía) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **4.2** `ensure_tools` (S33): RED (env con PATH mínimo sin ffmpeg/ffprobe → rc 1, mensaje claro, sin procesar archivos) → GREEN (`shutil.which` de ffmpeg Y ffprobe tras parsear, antes del bucle de archivos) → TRIANGULATE (solo falta ffprobe: mismo contrato) → REFACTOR. <!-- sdd-owner: implementation -->

## Fase 5 — Transcripción, traducción y progreso + sección real opt-in (design §9.5, §4.2/§4.4/§4.6)

- [x] **5.1** Gate opt-in en `test_smoke.py` (D8; habilita S6/S10/S11/S12): RED (sin `SUBTITLER_SMOKE_REAL` los checks reales reportan `SKIP (opt-in: SUBTITLER_SMOKE_REAL=1)` y NO cuentan como fallo, rc 0; con valor exacto `"1"` la sección real corre y sus fallos cuentan en `RESULT`) → GREEN (lectura estricta `== "1"` solo en el smoke; `subtitler.py` jamás lee la variable) → TRIANGULATE (`SUBTITLER_SMOKE_REAL=0` NO es opt-in) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **5.2** Progreso en una línea (S12, costura): RED (unit de la matemática pura: `pct = segment.end / duration`, `speed = segment.end / elapsed`, `ETA = elapsed * (1 - pct) / pct`; render `\r[####----]  45.0%  speed 1.53x  ETA 03:21` con `PROGRESS_BAR_WIDTH`=24 e intervalo mínimo 0.1 s; `say()` final para el salto) → GREEN → TRIANGULATE (pct 0 y 1, elapsed 0 sin división por cero) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **5.3** `transcribe_cues` lazy (S10/S11/S12): RED en suite default (import perezoso: unit verifica que `import subtitler` NO carga `faster_whisper`) → GREEN (`WhisperModel(model, device=spec.device, compute_type=spec.compute_type)`; `transcribe(str(path), language=None si auto, vad_filter=True, beam_size=WHISPER_BEAM_SIZE)`; `strip()` por segmento; un `Cue` por segmento; callback de progreso de 5.2; retorna `(cues, idioma_detectado)`) + checks reales opt-in: S11 (voz SAPI «hello world, this is a test» muxeada → `--model tiny` → SRT con ≥1 cue no vacío), S12 real (stdout contiene `ETA` y el `\r` no genera una línea por segmento), S10 e2e solo si hay voz TTS española instalada (residual §6.3). Verificación: la suite default sigue verde sin red. <!-- sdd-owner: implementation -->
- [x] **5.4** Traducción Argos (S6, contrato S14–S16): RED en suite default (`build_translator` con ruta `none` → identidad sin importar argostranslate; contrato WARN del caller: archivo ok, no failed) → GREEN (`installed_argos_pairs` lazy desde `package.get_installed_packages()`; directo 1 salto; pivote 2 saltos encadenados; «traduciendo N segmentos…») + check real opt-in S6 (pista `eng` → extracción + Argos `en→es` → SRT español válido no vacío). Verificación: suite default verde sin red; calidad de traducción fuera de alcance (riesgo aceptado en proposal). <!-- sdd-owner: implementation -->
- [x] **5.5** (enmienda, D13) Descarga Argos en demanda: RED (`unit A1`: ruta `none` + descarga exitosa → re-resuelve y traduce, con candidatos exactos directo-primero; `unit A2`: fallo de descarga / idioma desconocido → WARN + sin traducción; `unit A3`: directo preferido en el índice, one-time sin re-consulta, patas del pivote cuando el directo falta, nada disponible y excepción de instalación → `False` sin lanzar) → GREEN (`download_candidates` sin duplicados ni `(x,x)`; `_route_satisfiable`; `ensure_pairs_installed` con `update_package_index()` una vez e `install_from_path(p.download())`, lazy D3; `TranslationOutcome`) → TRIANGULATE (`download_candidates("en","es")` = solo el directo; 5.4 e2e offline real con aislamiento → WARN + solo sufijado) → REFACTOR. Notas: argostranslate 1.11 recursiona sin índice local (`RecursionError` capturado por contrato); el unit A3 llama `ensure_pairs_installed` directo y necesita stdio UTF-8 (la flecha → del INFO). <!-- sdd-owner: implementation -->
- [x] **5.6** (enmienda, D14) Doble salida `<stem>-<lang>.srt` + `<stem>.srt`: RED (5.4/5.5: fallo → SOLO `s16 eng-en.srt` con el stem completo, sin principal; e2e fake-argos: `en→es` con índice `[[("en","es")]` → `video.srt` traducido + `video-en.srt` original + INFO de descarga, sin `.tmp`; índice vacío → WARN + solo sufijado; origen `spa` → único `video.srt` sin duplicado) → GREEN (`ProducedSubtitles(original, final, lang, attempted)`; `subtitlar_one` escribe sufijada primero y principal después, atómico, limpieza de ambas ante fallo; SKIP/--force intactos sobre el principal; línea OK con salidas) → TRIANGULATE (rc 0 nunca aborta por traducción; `-orig` cubierto por contract en `ProducedSubtitles.lang=None` → sufijo) → REFACTOR. <!-- sdd-owner: implementation -->
- [x] **5.7** (enmienda) Documentación del cambio: spec R4 (descarga en demanda) + Escritura/nombres (doble salida, `-orig`, fallo) en RFC 2119; design §10 con D13/D14; apply-progress con evidencia TDD de la enmienda. Sin TDD (docs); verificación: coherencia spec ↔ design ↔ suite (escenarios 31 → 37). <!-- sdd-owner: implementation -->

## Fase 6 — README y verificación final (design §9.5–9.6)

- [x] **6.1** `README.md` en español (criterio 8 del proposal): uso y ejemplos, tabla de opciones, pipeline por archivo, modelos y tiempos estimados, limitaciones (Argos no rioplatense, bitmap → transcribe, residuales §6.3), códigos de salida 0/1/130 y 2 de uso, opt-in `SUBTITLER_SMOKE_REAL=1` (descarga one-time ~75 MB con `tiny`), versión de Python del venv (dato de 1.1), hoja de ruta e historial. Sin TDD (docs); verificación: contrastar contra `subtitler.py --help` real y la superficie implementada. <!-- sdd-owner: implementation -->
- [x] **6.2** Verificación final del cambio: `.venv/Scripts/python.exe test_smoke.py` verde sin red (suite default completa, `RESULT: N passed, 0 failed`, rc 0), `.venv/Scripts/python.exe -m py_compile subtitler.py` ok, y corrida opt-in `SUBTITLER_SMOKE_REAL=1 .venv/Scripts/python.exe test_smoke.py` documentada (solo si se acepta la descarga one-time). Confirmar que `git status` solo muestra los archivos del cambio. <!-- sdd-owner: implementation -->

## Acciones del orquestador (post-apply, agrupadas)

- [x] Antes de cerrar apply/merge: pausar y preguntar la decisión de entrega (ask-on-risk) — resuelto 2026-09-13: usuario eligió encadenar PR 1 → PR 2 → PR 3, chain `stacked-to-main`. <!-- sdd-owner: parent -->
- [x] Start or reuse bounded review — resuelto 2026-09-13: review switch desactivado por política del usuario (RDD off); entrega bajo política ordinaria de repositorio, sin review nativo. <!-- sdd-owner: parent -->
